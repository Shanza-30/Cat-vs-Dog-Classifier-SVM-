# Cat-vs-Dog-Classifier-SVM-
Cat vs Dog Classifier using SVM – An image classification project that uses Support Vector Machines (SVM) to classify cats and dogs from images. Includes preprocessing, scaling, PCA visualization, model training, evaluation, and test prediction with visual outputs.
